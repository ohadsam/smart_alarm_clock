<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- The two exact-alarm permissions are a pair, and the split matters.
         SCHEDULE_EXACT_ALARM is the user-revocable one introduced in API 31; from
         API 33 on it is superseded by USE_EXACT_ALARM, which is granted at install
         and cannot be revoked, and which Google Play allows precisely for apps whose
         core function is an alarm clock. Requesting the revocable one on 33+ as well
         is redundant — it asks for a permission the app already has by another route,
         and it is the kind of over-broad declaration Play review flags. maxSdkVersion
         confines it to the levels where it is the only option.
         Nothing branches on this: every call site goes through
         ReliabilityChecks.canScheduleExactAlarms(), which returns true on 33+. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <application
        android:name=".SmartRingApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.SmartRing"
        android:supportsRtl="true"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".receiver.AlarmReceiver"      android:exported="false"/>
        <receiver android:name=".service.StopAlarmReceiver"   android:exported="false"/>
        <receiver android:name=".service.SnoozeAlarmReceiver" android:exported="false"/>

        <receiver android:name=".receiver.BootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <!-- Alarms are armed as absolute timestamps computed from the local
                     wall clock, so both of these invalidate every one of them. -->
                <action android:name="android.intent.action.TIME_SET"/>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
                <!-- API 31+. Revoking SCHEDULE_EXACT_ALARM makes the system cancel
                     every exact alarm this app has armed; granting it back does not
                     restore them. Delivered explicitly to this package, so the
                     implicit-broadcast restrictions don't apply. -->
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED"/>
            </intent-filter>
        </receiver>

        <service android:name=".service.AlarmFiringService"
            android:exported="false" android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="alarm"/>
        </service>

        <!--
            The Quick Settings tile. exported="true" is required — the system's own Quick
            Settings UI binds it from outside this app, so an unexported tile simply never
            appears in the tile picker. The BIND_QUICK_SETTINGS_TILE permission is what
            keeps that safe: only the platform holds it, so nothing else can bind here.
        -->
        <service android:name=".service.AlarmsTileService"
            android:exported="true"
            android:icon="@drawable/ic_alarm"
            android:label="@string/qs_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <receiver android:name=".presentation.widget.SmartRingWidgetSmallReceiver"
            android:exported="false" android:label="@string/widget_small">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE"/></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_small_info"/>
        </receiver>
        <receiver android:name=".presentation.widget.SmartRingWidgetMediumReceiver"
            android:exported="false" android:label="@string/widget_medium">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE"/></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_medium_info"/>
        </receiver>
        <receiver android:name=".presentation.widget.SmartRingWidgetWideReceiver"
            android:exported="false" android:label="@string/widget_wide">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE"/></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_wide_info"/>
        </receiver>
        <receiver android:name=".presentation.widget.SmartRingWidgetLargeReceiver"
            android:exported="false" android:label="@string/widget_large">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE"/></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_large_info"/>
        </receiver>

        <provider android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false">
            <meta-data android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" tools:node="remove"/>
        </provider>
    </application>
</manifest>
